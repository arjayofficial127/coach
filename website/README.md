# Coach Browser website

- `frontend/` is the Vercel-ready one-page marketing site.
- `backend/` is reserved for the future backend and intentionally contains no runtime yet.

See `frontend/README.md` for local development and deployment notes.
