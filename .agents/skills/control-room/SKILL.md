---
name: control-room
description: Coordinate a focused Coach Browser repository task with local deterministic discovery, proportional validation, preserved user intent and evidence-based handoff. Use when the user invokes Control Room or asks to route a meaningful task in this repository.
---

Use this repository's AGENTS instructions. Read `.codex/WORKFLOW.md` for meaningful tasks;
read `.codex/PROJECT.md` and only relevant MAP pointers when orientation is needed.

Classify the requested mode before edits. Run `node scripts/ai/control-room.mjs doctor` when
environment status is needed, then `context` with focused scope/terms. These are local helper
commands, not Codex built-ins. Use Node 24+; README documents the existing Windows runtime.

Work inline for micro tasks. For persistent work capture a task baseline before edits using
`task`, retain the original request, and review the packet's criteria and protected behavior.
One implementation writer per checkout; native delegation only when useful and exposed. Give
workers a compact packet and no recursive delegation by default. Keep visual work with a client
that can see the actual inputs. Never change model/security settings for token savings.

Run appropriate reviewed `check` IDs and `handoff`; disclose unavailable visual checks, stale
evidence, uncertainty and open criteria. Follow the repository's scoped commit/queue rules after
gates pass. Persist only durable, supported knowledge. Hooks/skills do not guarantee enforcement.
